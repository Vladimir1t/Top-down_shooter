#all settings files need to be parced by server and client before game actually starts
#contains:

#object class
#unique object id (name)
#path to sprites (need to be uniform for all objects of one class, or speccially marked)
#any other internal settings

#name
#id
#default speed.x speed.y speed.rot
#starting x, y, rot
#starting vx, vy, rot

#count of sprites in each direction, each sprite frame duration
#up_sprites
#down_sprites
#left_spriets
#right_sprites
